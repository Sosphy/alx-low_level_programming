#include "main.h"

/**
 * string_toupper - function that changes all lowercase letters of a screen to uppercase letters
 * @*: pointer to screen
 * Return: pointer to uppercase string
 */

char *string_toupper(char *)
{
	int length_of_string;

	length_of_string = 0;

	while (*[length_of_string] != '\0'])
	{
		if (*[length_of_string] >= 97 && *[length_of_string] <= 122)
		{
			*[length_of_string] = *[length_of_string] - 32;
		}
		length_of_string++;
	}
	return (*)
}
